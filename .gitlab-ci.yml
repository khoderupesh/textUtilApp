build project:
  stage: build
  image: node:18.17.1
  script:
    - npm install
    - npm run build

netlify:
  stage: deploy
  image: node:15
  script:
    - npm install -g netlify-cli
    - netlify deploy --dir=build --prod